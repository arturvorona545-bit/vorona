# Задача №1 (Тема: Создание и вызов функций)
# Напишите простую функцию:
# 1. Создайте функцию с именем `say_hello`
# 2. Функция не должна принимать аргументы
# 3. Внутри функции выведите строку: "Привет, мир!"
# 4. Вызовите эту функцию один раз



def  say_hello() :
    print("Привет, мир!")

say_hello()

# Задача №2 (Тема: Функции с аргументами)
# Напишите функцию, которая принимает аргумент:
# 1. Создайте функцию с именем `greet_user`
# 2. Функция должна принимать один аргумент `name`
# 3. Внутри функции выведите: "Привет, [name]!"
# 4. Вызовите функцию два раза с разными именами
#    Например: greet_user("Анна") и greet_user("Петр")

def greet_user(name) :
    print(f"Привет, {name}!")
greet_user("Анна")
greet_user("Петр")


# Задача №3 (Тема: Возвращение данных из функций)
# Напишите функцию, которая возвращает результат:
# 1. Создайте функцию с именем `multiply`
# 2. Функция должна принимать два аргумента: `a` и `b`
# 3. Внутри функции умножьте `a` на `b` и верните результат
# 4. Вызовите функцию с числами 5 и 3
# 5. Сохраните результат в переменную и выведите его

def multiply(a,b) :
    return a*b
result = multiply(5,3)
print(result)



# Задача №4 (Тема: Аргументы с дефолтными значениями)
# Напишите функцию с аргументом по умолчанию:
# 1. Создайте функцию с именем `greet`
# 2. Функция должна принимать один аргумент `name`
# 3. Добавьте второй аргумент `greeting` со значением по умолчанию "Привет"
# 4. Внутри функции выведите: "[greeting], [name]!"
# 5. Вызовите функцию два раза:
#    - greet("Мария") - использует значение по умолчанию
#    - greet("Иван", "Здравствуй") - использует переданное значение

def greet(name, greeting ="Привет") :
    print(f"{greeting}, {name}!")
greet("Мария")
greet("Иван", "Здравствуй")


# Задача №5 (Тема: Декораторы)
# Напишите простой декоратор:
# 1. Создайте декоратор с именем `simple_decorator`
# 2. Декоратор должен добавлять строку "Функция вызвана" перед вызовом функции
# 3. Создайте функцию `say_hello`, которая выводит "Привет!"
# 4. Примените декоратор к функции `say_hello`
# 5. Вызовите функцию `say_hello`

def simple_decorator(func):
    def wrapper() :
        print("Функция вызвана")
        func()
    return wrapper
def say_hello():
    print("Привет!")
say_hello = simple_decorator(say_hello)
say_hello()


# Задача №6 (Тема: Функции с позиционными и именованными аргументами)
# Напишите функцию с разными типами аргументов:
# 1. Создайте функцию с именем `show_info`
# 2. Функция должна принимать три аргумента: `name`, `age`, `city`
# 3. Выведите информацию в формате: "Имя: [name], Возраст: [age], Город: [city]"
# 4. Вызовите функцию тремя способами:
#    - Только позиционными аргументами
#    - Только именованными аргументами
#    - Смешанным способом (позиционные + именованные)

def show_info(name,age,city):
    print(f"Имя:{name}, Возраст: {age}, Город: {city}")
show_info("Анна", 25, "Москва")
show_info(name="Иван", age=30, city="Санкт-Петербург")
show_info("Мария", city="Казань", age=28)

# Задача №7 (Тема: Возвращение данных из функций)
# Напишите функцию, которая возвращает отформатированную строку:
# 1. Создайте функцию с именем `create_email`
# 2. Функция должна принимать два аргумента: `username` и `domain`
# 3. Функция должна возвращать строку в формате email: "username@domain.com"
# 4. Вызовите функцию с аргументами "ivan" и "example"
# 5. Сохраните результат в переменную и выведите её

def create_email(username, domain) :
    return(f"{username}@{domain}.com")
result = create_email("ivan", "example")
print(result)


# Задача №8 (Тема: Аргументы с дефолтными значениями)
# Напишите функцию с несколькими аргументами по умолчанию:
# 1. Создайте функцию с именем `create_message`
# 2. Функция должна принимать три аргумента:
#    - `name` (обязательный)
#    - `greeting` (по умолчанию "Привет")
#    - `punctuation` (по умолчанию "!")
# 3. Функция должна возвращать строку: "[greeting], [name][punctuation]"
# 4. Вызовите функцию три раза:
#    - Только с именем
#    - С именем и приветствием
#    - Со всеми аргументами

def create_message(name, greeting="Привет", punctuation="!"):
    return(f"{greeting}, {name}{punctuation}")
print(create_message("Анна"))
print(create_message("Иван", "Здравствуй"))
print(create_message("Мария", "Добрый день", "."))

# Задача №9 (Тема: Создание и вызов функций)
# Простая функция с одним действием:
# 1. Создайте функцию с именем `show_date`
# 2. Функция не должна принимать аргументы
# 3. Внутри функции выведите текущую дату в формате "Сегодня: 2024-01-15"
#    (можно использовать фиксированную дату для простоты)
# 4. Вызовите функцию два раза

def show_date() :
    print(f"Сегодня:2024-01-15")
show_date()
show_date()


# Задача №10 (Тема: Функции с аргументами)
# Простая функция с одним аргументом:
# 1. Создайте функцию с именем `square_number`
# 2. Функция должна принимать один аргумент `num`
# 3. Внутри функции выведите квадрат числа в формате: "Квадрат числа [num] равен [результат]"
# 4. Вызовите функцию с числами 4 и 7

def square_number(num):
    print(f"Квадрат числа {num} равен {num**2}")
square_number(4)
square_number(7)

# Задача №11 (Тема: Возвращение данных из функций)
# Простая функция с возвратом значения:
# 1. Создайте функцию с именем `add_numbers`
# 2. Функция должна принимать два аргумента: `a` и `b`
# 3. Внутри функции сложите `a` и `b` и верните результат
# 4. Вызовите функцию с числами 10 и 5
# 5. Сохраните результат в переменной `sum_result` и выведите её

def add_numbers(a, b):
    return a+b
sum_result = add_numbers(10,5)

print(sum_result)


# Задача №12 (Тема: Аргументы с дефолтными значениями)
# Простая функция с дефолтным значением:
# 1. Создайте функцию с именем `welcome`
# 2. Функция должна принимать два аргумента:
#    - `name` (обязательный)
#    - `message` (по умолчанию "Добро пожаловать!")
# 3. Внутри функции выведите: "[name], [message]"
# 4. Вызовите функцию два раза:
#    - Только с именем "Анна"
#    - С именем "Иван" и сообщением "Приветствуем!"

def welcome(name,message="Добро пожаловать!"):
    print(f"{name}, {message}")
welcome("Анна")
welcome("Иван","Приветствуем")


# Задача №13 (Тема: Декораторы)
# Создайте простой декоратор:
# 1. Создайте декоратор с именем `my_decorator`
# 2. Декоратор должен выводить "Начало работы" перед вызовом функции
# 3. Создайте функцию `test_func`, которая выводит "Тестовая функция"
# 4. Примените декоратор к функции
# 5. Вызовите декорированную функцию

def my_decorator(func):
    def wrapper():
        print("Начало работы")
        func()
    return wrapper

@my_decorator
def test_func():
    print("Тестовая функция")

test_func()

# Задача №14 (Тема: Создание и вызов функций)
# Простая функция без аргументов:
# 1. Создайте функцию с именем `show_time`
# 2. Функция не должна принимать аргументы
# 3. Внутри функции выведите строку: "Текущее время: 14:30"
#    (можно использовать фиксированное время для простоты)
# 4. Вызовите функцию три раза

def show_time() :
    print("Текущее время: 14:30")
show_time()
show_time()
show_time()

# Задача №15 (Тема: Возвращение данных из функций)
# Простая функция с возвратом результата:
# 1. Создайте функцию с именем `multiply_by_two`
# 2. Функция должна принимать один аргумент `number`
# 3. Внутри функции умножьте число на 2 и верните результат
# 4. Вызовите функцию с числами 8 и 12
# 5. Сохраните результаты в переменные и выведите их

def multiply_by_two(number) :
    return number * 2
numb1 = multiply_by_two(8)
numb2 = multiply_by_two(12)

print(numb1, numb2)

# Задача №16 (Тема: Аргументы функций - позиционные и именованные)
# Простая функция с разными способами вызова:
# 1. Создайте функцию с именем `print_user`
# 2. Функция должна принимать три аргумента: `name`, `age`, `city`
# 3. Внутри функции выведите: "Пользователь: [name], [age] лет, г. [city]"
# 4. Вызовите функцию двумя способами:
#    - Позиционными аргументами: "Мария", 25, "Москва"
#    - Именованными аргументами: name="Иван", city="СПб", age=30

def print_user(name, age, city) :
    print(f"{name}, {age}лет, г. {city}")
print_user("Мария", 25, "Москва")
print_user(name="Иван", city="СПБ", age=30)


# Создайте функцию, которая возвращает результат вычисления:
# 1. Создайте функцию с именем `calculate_difference`
# 2. Функция должна принимать два аргумента: `a` и `b`
# 3. Внутри функции вычислите разность a - b и верните результат
# 4. Вызовите функцию с числами 20 и 7
# 5. Сохраните результат в переменную `diff` и выведите её

def calculate_difference(a,b) :
    return a-b
diff = calculate_difference(20,7)
print(diff)


# Задача №18 (Тема: Создание и вызов функций)
# Простая функция с повторяющимся действием:
# 1. Создайте функцию с именем `print_line`
# 2. Функция не должна принимать аргументы
# 3. Внутри функции выведите строку: "----------" (10 дефисов)
# 4. Вызовите функцию четыре раза

def print_line() :
    print("----------")
print_line()
print_line()
print_line()
print_line()


# Задача №19 (Тема: Аргументы функций - позиционные и именованные)
# Функция с разными способами передачи аргументов:
# 1. Создайте функцию с именем `format_date`
# 2. Функция должна принимать три аргумента: `day`, `month`, `year`
# 3. Внутри функции верните строку в формате: "Дата: [day].[month].[year]"
# 4. Вызовите функцию двумя способами:
#    - Только позиционными аргументами: 15, 3, 2024
#    - Только именованными аргументами: year=2023, month=12, day=31
# 5. Выведите оба результата

def format_date(day, month, year) :
    return f"Дата:{day}.{month}.{year}"
result1 = format_date(15,3,2024)
result2 = format_date(year=2023, month=12, day=31)
print(result1)
print(result2)


# Задача №20 (Тема: Возвращение данных из функций)
# Функция с возвратом вычисленного значения:
# 1. Создайте функцию с именем `get_average`
# 2. Функция должна принимать два аргумента: `num1` и `num2`
# 3. Внутри функции вычислите среднее арифметическое (сумма / 2) и верните результат
# 4. Вызовите функцию с числами 10 и 20
# 5. Сохраните результат в переменную `avg` и выведите её с сообщением "Среднее:"

def get_average(num1, num2) :
    return (num1+num2)/2
avg = get_average(10,20)
print(avg)

# Задача №21 (Тема: Создание и вызов функций)
# Простая функция с выводом информации:
# 1. Создайте функцию с именем `show_course`
# 2. Функция не должна принимать аргументы
# 3. Внутри функции выведите строку: "Курс: Python для автоматизации"
# 4. Вызовите функцию два раза

def show_course():
    print("Курс: Python для автоматизации")
show_course()
show_course()


# Задача №22 (Тема: Аргументы функций - позиционные и именованные)
# Функция с разными способами передачи данных:
# 1. Создайте функцию с именем `create_product_info`
# 2. Функция должна принимать три аргумента: `name`, `price`, `category`
# 3. Внутри функции верните строку: "Товар: [name], Цена: [price] руб., Категория: [category]"
# 4. Вызовите функцию тремя способами:
#    - Позиционные: "Монитор", 15000, "Электроника"
#    - Именованные: category="Книги", name="Питон для начинающих", price=1200
#    - Смешанные: "Мышь", price=2500, category="Аксессуары"
# 5. Выведите все три результата

def create_product_info(name, price, category) :
    return(f"Товар:{name}, Цена:{price}, Категория:{category}")
result1 = create_product_info("Монитор", 15000, "Электроника")
result2 = create_product_info(category="Книги", name="Питон для начинающих", price=1200)
result3 = create_product_info("Мышь", price=2500, category="Аксессуары")
print(result1)
print(result2)
print(result3)

# Задача №23 (Тема: Возвращение данных из функций)
# Функция с возвратом преобразованных данных:
# 1. Создайте функцию с именем `convert_to_uppercase`
# 2. Функция должна принимать один аргумент `text`
# 3. Внутри функции преобразуйте текст в верхний регистр и верните результат
# 4. Вызовите функцию со строкой "hello world"
# 5. Сохраните результат в переменную `uppercase_text` и выведите её

def convert_to_uppercase(text):
    return text.upper()
uppercase_text = (convert_to_uppercase("hello world"))
print(uppercase_text)

# Задача №24 (Тема: Создание и вызов функций)
# Простая функция с выводом приветствия:
# 1. Создайте функцию с именем `morning_greeting`
# 2. Функция не должна принимать аргументы
# 3. Внутри функции выведите строку: "Доброе утро! Хорошего дня!"
# 4. Вызовите функцию три раза

def morning_greeting() :
    print("Доброе утро! Хорошего дня!")
morning_greeting()
morning_greeting()
morning_greeting()

# Задача №25 (Тема: Аргументы функций - позиционные и именованные)
# Функция с разными комбинациями аргументов:
# 1. Создайте функцию с именем `build_url`
# 2. Функция должна принимать три аргумента: `protocol`, `domain`, `page`
# 3. Внутри функции верните строку в формате: "[protocol]://[domain]/[page]"
# 4. Вызовите функцию тремя способами:
#    - Все позиционные: "https", "example.com", "index.html"
#    - Все именованные: page="about.html", protocol="http", domain="site.com"
#    - Смешанные: "ftp", domain="files.com", page="download.zip"
# 5. Выведите все три результата

def build_url(protocol, domain, page):
    return f"{protocol}://{domain}{page}"
result1 = build_url("http", "example.com", "index.html")
result2 = build_url(page="about.html", protocol="http", domain="site.com")
result3 = build_url("ftp", domain="files.com", page="download.zip")
print(result1)
print(result2)
print(result3)

# Задача №26 (Тема: Аргументы с дефолтными значениями)
# Функция с одним дефолтным аргументом:
# 1. Создайте функцию с именем `greet_person`
# 2. Функция должна принимать два аргумента:
#    - `name` (обязательный)
#    - `time_of_day` (по умолчанию "день")
# 3. Внутри функции выведите: "Добрый [time_of_day], [name]!"
# 4. Вызовите функцию два раза:
#    - Только с именем "Анна"
#    - С именем "Иван" и временем "вечер"

def greet_person(name,time_of_day="день") :
    print(f"Добрый {time_of_day}, {name}!")
greet_person("Анна")
greet_person("Иван","вечер")

# Задача №27 (Тема: Аргументы с дефолтными значениями)
# Функция с несколькими дефолтными аргументами:
# 1. Создайте функцию с именем `create_profile`
# 2. Функция должна принимать три аргумента:
#    - `username` (обязательный)
#    - `role` (по умолчанию "user")
#    - `status` (по умолчанию "active")
# 3. Внутри функции верните строку: "Профиль: [username], Роль: [role], Статус: [status]"
# 4. Вызовите функцию три раза:
#    - Только с username "admin"
#    - С username "moderator" и role "moderator"
#    - Со всеми аргументами: username="guest", status="inactive"

def create_profile(username,role="user",status="active") :
    return(f"Профиль: {username}, Роль:{role}, Статус:{status}")
print(create_profile("admin"))
print(create_profile(username="moderator", role="moderator"))
print(create_profile(username="guest",status="inactive"))

# Задача №28 (Тема: Аргументы с дефолтными значениями)
# Функция со смешанными обязательными и дефолтными аргументами:
# 1. Создайте функцию с именем `order_info`
# 2. Функция должна принимать три аргумента:
#    - `product` (обязательный)
#    - `quantity` (обязательный)
#    - `discount` (по умолчанию 0)
# 3. Внутри функции вычислите стоимость: quantity * 100 * (1 - discount/100)
#    (цена товара 100, discount - процент скидки)
# 4. Верните строку: "Товар: [product], Количество: [quantity], Стоимость: [результат] руб."
# 5. Вызовите функцию три раза:
#    - Без скидки: "Книга", 2
#    - Со скидкой 10%: "Ручка", 5, discount=10
#    - Со скидкой 20%: "Блокнот", 3, 20

def order_info(product,quantity,discount=0):
    quantity1 = quantity * 100 * (1 - discount/100)
    return(f"Товар: {product}, Количество: {quantity}, Стоимость: {quantity1} руб.")
print(order_info("Книга",2))
print(order_info("Ручка",5, discount=10))
print(order_info("Блокнот", 3, 20))

# Задача №29 (Тема: Аргументы с дефолтными значениями)
# Функция с дефолтным аргументом-списком:
# 1. Создайте функцию с именем `add_to_list`
# 2. Функция должна принимать два аргумента:
#    - `item` (обязательный)
#    - `items_list` (по умолчанию пустой список [])
# 3. Внутри функции добавьте `item` в `items_list`
# 4. Верните измененный список
# 5. Вызовите функцию три раза:
#    - add_to_list("яблоко")
#    - add_to_list("банан", ["апельсин"])
#    - add_to_list("груша")

def add_to_list(item,items_list=[]):
    items_list.append(item)
    return items_list
print(add_to_list("яблоко"))
print(add_to_list("банан", ["апельсин"]))
print(add_to_list("груша"))

# Задача №30 (Тема: Аргументы с дефолтными значениями)
# Функция с дефолтным числовым аргументом:
# 1. Создайте функцию с именем `calculate_price`
# 2. Функция должна принимать три аргумента:
#    - `price` (обязательный)
#    - `quantity` (обязательный)
#    - `tax` (по умолчанию 20)
# 3. Внутри функции вычислите итоговую стоимость: price * quantity * (1 + tax/100)
# 4. Верните результат вычисления
# 5. Вызовите функцию три раза:
#    - Без указания tax: price=100, quantity=2
#    - С tax=10: price=200, quantity=3, tax=10
#    - С tax=0: price=50, quantity=5

def calculate_price(price,quantity,tax=20):
    return(price * quantity * (1 + tax/100))
print(calculate_price(price=100, quantity=2))
print(calculate_price(price=200, quantity=3, tax=10))
print(calculate_price(price=50, quantity=5))

# Простой декоратор с выводом сообщения:
# 1. Создайте декоратор с именем `simple_decor`
# 2. Декоратор должен выводить "--- Начало ---" перед вызовом функции
# 3. Создайте функцию `test_func`, которая выводит "Тестовая функция"
# 4. Примените декоратор к функции
# 5. Вызовите декорированную функцию

def  simple_decor(func):
    def wrapper():
        print("--- Начало ---")
        func()
    return wrapper
@simple_decor
def test_func():
    print("Тестовая функция")
test_func()

# Задача №33 (Тема: Декораторы)
# Очень простой декоратор:
# 1. Создайте декоратор с именем `hello_decorator`
# 2. Декоратор должен выводить "Привет!" перед вызовом функции
# 3. Создайте функцию `say_name`, которая выводит "Меня зовут Python"
# 4. Примените декоратор к функции
# 5. Вызовите декорированную функцию

def hello_decorator(func):
    def wrapper():
        print("Привет!")
        func()
    return wrapper
@hello_decorator
def say_name():
    print("Меня зовут Python")
say_name()


# Задача №34 (Тема: Возвращение данных из функций)
# Простая функция с возвратом значения:
# 1. Создайте функцию с именем `double_number`
# 2. Функция должна принимать один аргумент `num`
# 3. Внутри функции умножьте число на 2 и верните результат
# 4. Вызовите функцию с числом 7
# 5. Сохраните результат в переменную `result` и выведите её

def double_number(num) :
    return num * 2
result = double_number(7)
print(result)


# Задача №35 (Тема: Создание и вызов функций)
# Простая функция без аргументов:
# 1. Создайте функцию с именем `print_course`
# 2. Функция не должна принимать аргументы
# 3. Внутри функции выведите строку: "Курс: Автоматизация на Python"
# 4. Вызовите функцию два раза

def print_course():
    print(f"Курс: Автоматизация на Python")
print_course()
print_course()


# Задача №36 (Тема: Аргументы функций)
# Функция с двумя аргументами:
# 1. Создайте функцию с именем `show_full_name`
# 2. Функция должна принимать два аргумента: `first_name` и `last_name`
# 3. Внутри функции выведите: "Полное имя: [first_name] [last_name]"
# 4. Вызовите функцию с аргументами "Иван" и "Иванов"

def show_full_name(first_name, last_name):
    print(f"Полное имя:{first_name} {last_name}")
show_full_name("Иван","Иванов")


# Задача №37 (Тема: Аргументы с дефолтными значениями)
# Функция с дефолтным аргументом:
# 1. Создайте функцию с именем `greet_user`
# 2. Функция должна принимать два аргумента:
#    - `name` (обязательный)
#    - `greeting` (по умолчанию "Привет")
# 3. Внутри функции выведите: "[greeting], [name]!"
# 4. Вызовите функцию два раза:
#    - Только с именем "Мария"
#    - С именем "Петр" и greeting="Здравствуйте"

def greet_user(name, greeting="Привет"):
    print(f"{greeting},{name}")
greet_user("Мария")
greet_user("Петр", greeting="Здравствуйте")

# Задача №38 (Тема: Возвращение данных из функций)
# Функция с возвратом преобразованного значения:
# 1. Создайте функцию с именем `make_lowercase`
# 2. Функция должна принимать один аргумент `text`
# 3. Внутри функции преобразуйте текст в нижний регистр и верните результат
# 4. Вызовите функцию со строкой "ПРИВЕТ МИР"
# 5. Сохраните результат в переменную `lower_text` и выведите её

def make_lowercase(text):
    return text.lower()
lower_text = make_lowercase("ПРИВЕТ МИР")
print(lower_text)


# Задача №39 (Тема: Создание и вызов функций)
# Простая функция с повторяющимся выводом:
# 1. Создайте функцию с именем `print_separator`
# 2. Функция не должна принимать аргументы
# 3. Внутри функции выведите строку: "==========" (10 знаков =)
# 4. Вызовите функцию три раза

def print_separator():
    print("==========")
print_separator()
print_separator()
print_separator()

# Задача №40 (Тема: Аргументы функций - позиционные и именованные)
# Функция с разными способами вызова:
# 1. Создайте функцию с именем `create_record`
# 2. Функция должна принимать три аргумента: `id`, `name`, `value`
# 3. Внутри функции верните строку: "Запись #[id]: [name] = [value]"
# 4. Вызовите функцию двумя способами:
#    - Позиционные аргументы: 1, "Температура", 25
#    - Именованные аргументы: value=100, id=2, name="Давление"
# 5. Выведите оба результата

def create_record(id,name,value):
    return(f"Запись #{id}: {name} = {value}")
result = create_record(1, "Температура", 25)
result1 = create_record(value=100,id=2,name="Давление")
print(result)
print(result1)