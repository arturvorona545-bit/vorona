# Задача №1 (по условиям, из раздела: Оператор else)
# Напишите программу, которая принимает строку.
# Если строка равна "Python", выведите "Язык Python".
# В любом другом случае выведите "Другой язык".
# Используйте конструкцию if-else.
x = "Python"
if x == "Python":
    print("Язык Python")
else :
    print("Другой язык")

# Задача №2 (по условиям, из раздела: Логические операции в условиях)
 # Напишите программу, которая проверяет значение переменной `age`.
# Если возраст (`age`) больше или равен 18 И меньше или равен 65, выведите "Доступ разрешен".
# В противном случае выведите "Доступ запрещен".
# Используйте логическую операцию `and` в условии.

age = 45
if age >= 18 and age <= 65 :
    print("Доступ разрешен")
else :
    print("Доступ запрещен")

# Задача №3 (по условиям, из раздела: Оператор if)
# Напишите программу, которая проверяет значение переменной `number`.
# Если число положительное (больше нуля), выведите "Число положительное".
# Других действий в программе не требуется.

number = 1
if number > 0 :
    print("Число положительное")

# Задача №4 (по условиям, из раздела: Логические операции)
# Логическая операция `and` возвращает True только если ОБА условия истинны.
# Даны два условия: A = True и B = True.
# Создайте переменную `result`, которая будет хранить результат логической операции `and` между A и B.
# Выведите значение `result` на экран.
A = True
B = True
result = A and B

print(result)

# Задача №5 (по условиям, из раздела: Оператор else)
# Напишите программу, которая проверяет значение переменной `temperature`.
# Если температура меньше нуля, выведите "Холодно".
# В любом другом случае (else) выведите "Не холодно".

temperature = -5
if temperature < 0 :
    print("Холодно")
else :
    print("Не холодно")


# Задача №6 (по условиям, из раздела: Оператор elif)
# Оператор `elif` используется для проверки нескольких условий подряд.
# Напишите программу, которая проверяет значение переменной `score` (оценка):
# - Если `score` больше или равно 90, выведите "Отлично".
# - Иначе, если (`elif`) `score` больше или равно 70, выведите "Хорошо".
# - Иначе, если (`elif`) `score` больше или равно 50, выведите "Удовлетворительно".
# - Во всех остальных случаях (`else`) выведите "Неудовлетворительно".

score = 100
if score >= 90 :
    print("Отлично")
elif score >= 70 :
    print("Хорошо")
elif score >= 50 :
    print("Удовлетворительно")
else:
    print("Неудовлетворительно")

# Задача №7 (по условиям, из раздела: Логические операции в условиях)
# Напишите программу, которая проверяет значение переменной `x`.
# Если `x` больше 10 И при этом `x` меньше 20, выведите "x в диапазоне".
# В противном случае выведите "x вне диапазона".
# Используйте логическую операцию `and` в условии.

x = 15
if x > 10 and x < 20 :
    print("x в диапазоне")
else :
    print("x вне диапазона")

# Задача №8 (по условиям, из раздела: Оператор else)
# Напишите программу, которая проверяет строку `password`.
# Если пароль равен "secret123", выведите "Доступ разрешен".
# В любом другом случае выведите "Доступ запрещен".
# Используйте конструкцию if-else.

password = "secret123"
if password == "secret123" :
    print("Доступ разрешен")
else :
    print("Доступ запрещен")

# Задача №9 (по условиям, из раздела: Тернарный оператор)
# Тернарный оператор позволяет записать простое условие if-else в одну строку.
# Синтаксис: [значение_if_true] if [условие] else [значение_if_false]
# Напишите программу, которая проверяет значение переменной `is_member`.
# Если `is_member` равно True, присвойте переменной `status` строку "Пользователь является членом клуба".
# Если `is_member` равно False, присвойте переменной `status` строку "Пользователь не является членом клуба".
# Используйте тернарный оператор.
# В конце программы выведите значение `status`.

is_member = True
status = "Пользователь является членом клуба" if is_member else "Пользователь не является членом клуба"
print(status)

# Задача №10 (по условиям, из раздела: Логические операции)
# Даны две булевы переменные: `has_ticket = True` и `has_id = False`.
# Напишите выражение, которое проверяет, что билет ЕСТЬ ИЛИ удостоверение ЕСТЬ.
# Результат проверки (True или False) сохраните в переменную `can_enter` и выведите её на экран.
# Используйте логическую операцию `or`.

has_ticket = True
has_id = False
can_enter = has_ticket or has_id

print(can_enter)

# Задача №11 (по условиям, из раздела: Оператор if)
# Напишите программу, которая проверяет значение переменной `speed` (скорость).
# Если скорость превышает 60 (больше 60), выведите "Превышение скорости!".
# Других действий в программе не требуется.

speed = 70
if speed > 60 :
    print("Превышение скорости!")

# Задача №12 (по условиям, из раздела: Оператор elif)
# Напишите программу, которая проверяет значение переменной `time` (время суток в часах, от 0 до 23):
# - Если `time` меньше 12, выведите "Утро".
# - Иначе, если (`elif`) `time` меньше 18, выведите "День".
# - Иначе, если (`elif`) `time` меньше 22, выведите "Вечер".
# - Во всех остальных случаях (`else`) выведите "Ночь".

time = 11
if time < 12 :
    print("Утро")
elif time < 18 :
    print("День")
elif time < 22 :
    print("Вечер")
else :
    print("Ночь")

# Задача №13 (по условиям, из раздела: Логические операции в условиях)
# Напишите программу, которая проверяет два условия для переменной `num`.
# Если число положительное (больше нуля) И число четное (делится на 2 без остатка), выведите "Подходит".
# В противном случае выведите "Не подходит".
# Используйте логическую операцию `and` в условии.
# Проверку на чётность можно сделать через `num % 2 == 0`.

num = 2
if num > 0 and num % 2 == 0 :
    print("Подходит")
else:
    print("Не подходит")


