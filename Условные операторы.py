# Задача №1 (по условиям, из раздела: Оператор else)
# Напишите программу, которая принимает строку.
# Если строка равна "Python", выведите "Язык Python".
# В любом другом случае выведите "Другой язык".
# Используйте конструкцию if-else.
x = "Python"
if x == "Python":
    print("Язык Python")
else :
    print("Другой язык")

# Задача №2 (по условиям, из раздела: Логические операции в условиях)
 # Напишите программу, которая проверяет значение переменной `age`.
# Если возраст (`age`) больше или равен 18 И меньше или равен 65, выведите "Доступ разрешен".
# В противном случае выведите "Доступ запрещен".
# Используйте логическую операцию `and` в условии.

age = 45
if age >= 18 and age <= 65 :
    print("Доступ разрешен")
else :
    print("Доступ запрещен")

# Задача №3 (по условиям, из раздела: Оператор if)
# Напишите программу, которая проверяет значение переменной `number`.
# Если число положительное (больше нуля), выведите "Число положительное".
# Других действий в программе не требуется.

number = 1
if number > 0 :
    print("Число положительное")

# Задача №4 (по условиям, из раздела: Логические операции)
# Логическая операция `and` возвращает True только если ОБА условия истинны.
# Даны два условия: A = True и B = True.
# Создайте переменную `result`, которая будет хранить результат логической операции `and` между A и B.
# Выведите значение `result` на экран.
A = True
B = True
result = A and B

print(result)

# Задача №5 (по условиям, из раздела: Оператор else)
# Напишите программу, которая проверяет значение переменной `temperature`.
# Если температура меньше нуля, выведите "Холодно".
# В любом другом случае (else) выведите "Не холодно".

temperature = -5
if temperature < 0 :
    print("Холодно")
else :
    print("Не холодно")


# Задача №6 (по условиям, из раздела: Оператор elif)
# Оператор `elif` используется для проверки нескольких условий подряд.
# Напишите программу, которая проверяет значение переменной `score` (оценка):
# - Если `score` больше или равно 90, выведите "Отлично".
# - Иначе, если (`elif`) `score` больше или равно 70, выведите "Хорошо".
# - Иначе, если (`elif`) `score` больше или равно 50, выведите "Удовлетворительно".
# - Во всех остальных случаях (`else`) выведите "Неудовлетворительно".

score = 100
if score >= 90 :
    print("Отлично")
elif score >= 70 :
    print("Хорошо")
elif score >= 50 :
    print("Удовлетворительно")
else:
    print("Неудовлетворительно")

# Задача №7 (по условиям, из раздела: Логические операции в условиях)
# Напишите программу, которая проверяет значение переменной `x`.
# Если `x` больше 10 И при этом `x` меньше 20, выведите "x в диапазоне".
# В противном случае выведите "x вне диапазона".
# Используйте логическую операцию `and` в условии.

x = 15
if x > 10 and x < 20 :
    print("x в диапазоне")
else :
    print("x вне диапазона")

# Задача №8 (по условиям, из раздела: Оператор else)
# Напишите программу, которая проверяет строку `password`.
# Если пароль равен "secret123", выведите "Доступ разрешен".
# В любом другом случае выведите "Доступ запрещен".
# Используйте конструкцию if-else.

password = "secret123"
if password == "secret123" :
    print("Доступ разрешен")
else :
    print("Доступ запрещен")

# Задача №9 (по условиям, из раздела: Тернарный оператор)
# Тернарный оператор позволяет записать простое условие if-else в одну строку.
# Синтаксис: [значение_if_true] if [условие] else [значение_if_false]
# Напишите программу, которая проверяет значение переменной `is_member`.
# Если `is_member` равно True, присвойте переменной `status` строку "Пользователь является членом клуба".
# Если `is_member` равно False, присвойте переменной `status` строку "Пользователь не является членом клуба".
# Используйте тернарный оператор.
# В конце программы выведите значение `status`.

is_member = True
status = "Пользователь является членом клуба" if is_member else "Пользователь не является членом клуба"
print(status)

# Задача №10 (по условиям, из раздела: Логические операции)
# Даны две булевы переменные: `has_ticket = True` и `has_id = False`.
# Напишите выражение, которое проверяет, что билет ЕСТЬ ИЛИ удостоверение ЕСТЬ.
# Результат проверки (True или False) сохраните в переменную `can_enter` и выведите её на экран.
# Используйте логическую операцию `or`.

has_ticket = True
has_id = False
can_enter = has_ticket or has_id

print(can_enter)

# Задача №11 (по условиям, из раздела: Оператор if)
# Напишите программу, которая проверяет значение переменной `speed` (скорость).
# Если скорость превышает 60 (больше 60), выведите "Превышение скорости!".
# Других действий в программе не требуется.

speed = 70
if speed > 60 :
    print("Превышение скорости!")

# Задача №12 (по условиям, из раздела: Оператор elif)
# Напишите программу, которая проверяет значение переменной `time` (время суток в часах, от 0 до 23):
# - Если `time` меньше 12, выведите "Утро".
# - Иначе, если (`elif`) `time` меньше 18, выведите "День".
# - Иначе, если (`elif`) `time` меньше 22, выведите "Вечер".
# - Во всех остальных случаях (`else`) выведите "Ночь".

time = 11
if time < 12 :
    print("Утро")
elif time < 18 :
    print("День")
elif time < 22 :
    print("Вечер")
else :
    print("Ночь")

# Задача №13 (по условиям, из раздела: Логические операции в условиях)
# Напишите программу, которая проверяет два условия для переменной `num`.
# Если число положительное (больше нуля) И число четное (делится на 2 без остатка), выведите "Подходит".
# В противном случае выведите "Не подходит".
# Используйте логическую операцию `and` в условии.
# Проверку на чётность можно сделать через `num % 2 == 0`.

num = 2
if num > 0 and num % 2 == 0 :
    print("Подходит")
else:
    print("Не подходит")


# Задача №14 (Тема: Оператор else)
# Напишите программу, которая проверяет, является ли число четным или нечетным:
# 1. Создайте переменную `num` и присвойте ей значение 10
# 2. Используя оператор `if`, проверьте, делится ли `num` на 2 без остатка (остаток от деления равен 0)
# 3. Если число четное, выведите "Число четное"
# 4. Используя оператор `else`, для случая когда число нечетное, выведите "Число нечетное"

num = 10
if num %2 == 0 :
    print("Число четное")
else:
    print("Число нечетное")


# Задача №15 (Тема: Логические операции)
# Напишите программу, которая проверяет, может ли студент сдать экзамен:
# 1. Создайте переменную `attendance` (посещаемость) со значением 0.85 (85%)
# 2. Создайте переменную `grade` (оценка) со значением 4 (по 5-балльной шкале)
# 3. Используя логическую операцию `and`, проверьте два условия:
#   - посещаемость больше или равна 0.8 (80%)
#   - оценка больше или равна 3
# 4. Если ОБА условия истинны, выведите "Экзамен сдан"
# 5. Если хотя бы одно условие ложно, выведите "Экзамен не сдан"

attendance = 0.85
grade = 4

if attendance >= 0.8 and grade >= 3 :
    print("Экзамен сдан")
else :
    print("Экзамен не сдан")


# Задача №16 (Тема: Оператор elif)
# Напишите программу, которая определяет оценку по баллам:
# 1. Создайте переменную `score` и присвойте ей значение 85
# 2. Используя конструкцию `if-elif-else`, определите оценку:
#   - если `score` >= 90: выведите "Отлично"
#   - иначе если (`elif`) `score` >= 70: выведите "Хорошо"
#   - иначе если (`elif`) `score` >= 50: выведите "Удовлетворительно"
#   - во всех остальных случаях (`else`): выведите "Неудовлетворительно"

score = 85

if score >= 90 :
    print("Отлично")
elif score >= 70 :
    print("Хорошо")
elif score >= 50 :
    print("Удовлетворительно")
else:
    print("Неудовлетворительно")


# Задача №17 (Тема: Логические операции в условиях)
# Напишите программу, которая проверяет, может ли пользователь получить скидку:
# 1. Создайте переменную `is_member` (является ли членом клуба) со значением True
# 2. Создайте переменную `purchase_amount` (сумма покупки) со значением 1200
# 3. Проверьте сложное условие:
#    - пользователь является членом клуба ИЛИ сумма покупки больше 1000
# 4. Если условие истинно, выведите "Скидка доступна"
# 5. Если условие ложно, выведите "Скидка не доступна"

is_member = True
purchase_amount = 1200

if is_member or purchase_amount > 1000 :
    print("Скидка доступна")
else:
    print("Скидка не доступна")

# Задача №18 (Тема: Тернарный оператор)
# Напишите программу, которая использует тернарный оператор:
# 1. Создайте переменную `age` и присвойте ей значение 20
# 2. Используя тернарный оператор, определите строку `status`:
#    - если `age` >= 18, то `status` = "совершеннолетний"
#    - иначе `status` = "несовершеннолетний"
# 3. Выведите значение переменной `status`
# Формат тернарного оператора: [значение_if_true] if [условие] else [значение_if_false]

age = 20
status = "совершеннолетний" if age >= 18 else "несовершеннолетний"

print(status)


# Задача №19 (Тема: Логические операции)
# Напишите программу, которая проверяет два простых условия:
# 1. Создайте переменную `x` и присвойте ей значение 15
# 2. Создайте переменную `y` и присвойте ей значение 10
# 3. Проверьте с помощью оператора `if`:
#    - больше ли `x` чем `y` И больше ли `x` чем 0
# 4. Если оба условия истинны, выведите "Оба условия верны"
# 5. Если нет, ничего не выводите

x = 15
y = 10
if x > y and x > 0 :
    print("Оба условия верны")

# Задача №20 (Тема: Логические операции в условиях)
# Напишите программу, которая проверяет условия для входа в клуб:
# 1. Создайте переменную `age` и присвойте ей значение 19
# 2. Создайте переменную `has_id` и присвойте ей значение True
# 3. Проверьте сложное условие: возраст больше или равен 18 И наличие документа (has_id)
# 4. Если условие истинно, выведите "Проход разрешен"
# 5. Если условие ложно, выведите "Проход запрещен"

age = 19
has_id = True
if age >= 18 and has_id :
    print("Проход разрешен")
else:
    print("Проход запрещен")

# Задача №21 (Тема: Тернарный оператор)
# Напишите программу, которая использует тернарный оператор:
# 1. Создайте переменную `temperature` и присвойте ей значение 25
# 2. Используя тернарный оператор, определите строку `weather_status`:
#    - если `temperature` > 20, то `weather_status` = "Тепло"
#    - иначе `weather_status` = "Прохладно"
# 3. Выведите значение переменной `weather_status`
# Формат тернарного оператора: [значение_if_true] if [условие] else [значение_if_false]

temperature = 25
weather_status = "Тепло" if temperature > 20 else "Прохладно"

print(weather_status)

# Задача №22 (Тема: Оператор elif)
# Напишите программу, которая определяет категорию товара по цене:
# 1. Создайте переменную `price` и присвойте ей значение 1500
# 2. Используя конструкцию `if-elif-else`, определите категорию:
#    - если `price` < 500: выведите "Бюджетный"
#    - иначе если (`elif`) `price` < 2000: выведите "Средний"
#    - иначе если (`elif`) `price` < 5000: выведите "Премиум"
#    - во всех остальных случаях (`else`): выведите "Люкс"

price = 1500
if price < 500 :
    print("Бюджетный")
elif price < 2000 :
    print("Средний")
elif price < 5000 :
    print("Премиум")
else :
    print("Люкс")

# Задача №23 (Тема: Логические операции)
# Напишите программу, которая проверяет возможность получения кредита:
# 1. Создайте переменную `good_income` и присвойте ей значение True
# 2. Создайте переменную `good_credit_score` и присвойте ей значение False
# 3. Используя логическую операцию `or`, проверьте хотя бы одно условие:
#    - хороший доход ИЛИ хорошая кредитная история
# 4. Если хотя бы одно условие истинно, выведите "Кредит возможен"
# 5. Если оба условия ложны, выведите "Кредит невозможен"

good_income = True
good_credit_score = False
if good_income or good_credit_score :
    print("Кредит возможен")
else:
    print("Кредит невозможен")


#Задача №24 (Тема: Оператор if)
# Напишите программу, которая проверяет простое условие:
# 1. Создайте переменную `number` и присвойте ей значение 15
# 2. Используя оператор `if`, проверьте, делится ли число на 5 без остатка
# 3. Если условие истинно, выведите "Число делится на 5"
# 4. Если условие ложно, ничего не выводите

number = 15
if number % 5 == 0:
    print("Число делится на 5")


# Задача №25 (Тема: Оператор else)
# Напишите программу, которая проверяет доступ к контенту:
# 1. Создайте переменную `user_age` и присвойте ей значение 16
# 2. Используя оператор `if`, проверьте, больше ли возраст 18
# 3. Если возраст больше 18, выведите "Доступ разрешен"
# 4. Используя оператор `else`, для случая когда возраст 18 или меньше, выведите "Доступ запрещен"

user_age = 16
if user_age > 18 :
    print("Доступ разрешен")
else :
    print( "Доступ запрещен")

# Задача №26 (Тема: Логические операции)
# Выполните логические операции:
# 1. Создайте переменную `a` со значением True
# 2. Создайте переменную `b` со значением False
# 3. Выведите результат операции `a and b`
# 4. Выведите результат операции `a or b`
# 5. Выведите результат операции `not a`

a = True
b = False

print(a and b)
print(a or b)
print(not a)

# Задача №27 (Тема: Оператор if)
# Используйте простой оператор if:
# 1. Создайте переменную `age` со значением 18
# 2. Если `age` больше или равно 18, выведите "Доступ разрешен"

age = 18
if age >= 18 :
    print("Доступ разрешен")


# Задача №28 (Тема: Оператор if-else)
# Используйте оператор if-else:
# 1. Создайте переменную `temperature` со значением 15
# 2. Если `temperature` больше 20, выведите "Тепло"
# 3. Иначе выведите "Прохладно"

temperature = 15
if temperature > 20 :
    print("Тепло")
else:
    print("Прохладно")

# Задача №29 (Тема: Оператор elif)
# Используйте оператор elif для нескольких условий:
# 1. Создайте переменную `score` со значением 75
# 2. Если `score` >= 90, выведите "Отлично"
# 3. Если `score` >= 70, выведите "Хорошо"
# 4. Если `score` >= 50, выведите "Удовлетворительно"
# 5. Иначе выведите "Неудовлетворительно"

score = 75
if score >= 90 :
    print("Отлично")
elif score >= 70 :
    print("Хорошо")
elif score >= 50 :
    print("Удовлетворительно")
else :
    print("Неудовлетворительно")

# Задача №30 (Тема: Логические операции в условиях)
# Используйте логические операции в условиях:
# 1. Создайте переменные: `has_ticket = True` и `age = 16`
# 2. Если есть билет (has_ticket == True) И возраст >= 18, выведите "Вход разрешен"
# 3. Если есть билет И возраст < 18, выведите "Нужен сопровождающий"
# 4. Иначе выведите "Вход запрещен"

has_ticket = True
age = 16
if has_ticket == True and age >= 18 :
    print("Вход разрешен")
elif has_ticket == True and age < 18 :
    print("Нужен сопровождающий")
else:
    print("Вход запрещен")

# Задача №31 (Тема: Тернарный оператор)
# Используйте тернарный оператор:
# 1. Создайте переменную `number` со значением 7
# 2. С помощью тернарного оператора определите, является ли число четным или нечетным
# 3. Сохраните результат в переменную `result`
# 4. Выведите результат

number = 7
result = "четное" if number % 2 == 0 else "нечетное"
print(result)

