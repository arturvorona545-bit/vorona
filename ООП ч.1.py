# Задача №1 (Тема: Создание класса)
# Создайте простой класс:
# 1. Создайте класс с именем `Dog`
# 2. Внутри класса напишите ключевое слово `pass`
# 3. Класс пока не должен содержать никаких атрибутов и методов

class Dog :
    pass

# Задача №2 (Тема: Общие атрибуты)
# Создайте класс с общим атрибутом:
# 1. Создайте класс `Cat`
# 2. Добавьте общий атрибут класса `species` со значением "Кошка"
# 3. Выведите этот атрибут на экран (через обращение к классу)

class Cat:
    species = "Кошка"
print(Cat.species)

# Задача №3 (Тема: Конструктор класса __init__)
# Создайте класс с конструктором:
# 1. Создайте класс `Person`
# 2. Добавьте конструктор `__init__`, который принимает параметры `name` и `age`
# 3. Внутри конструктора сохраните эти параметры как атрибуты экземпляра
# 4. Создайте объект класса `Person` с именем "Ольга" и возрастом 28

class Person:
    def __init__(self,name,age):
        self.name = name
        self.age = age
my_person = Person("Ольга", 28)
print(my_person.name)
print(my_person.age)

# Задача №4 (Тема: Создание класса)
# Создайте пустой класс:
# 1. Создайте класс с именем `Book`
# 2. Класс пока не должен содержать никаких атрибутов и методов
# 3. Используйте `pass`

class Book:
    pass

# Задача №5 (Тема: Общие атрибуты)
# Создайте класс с общим атрибутом:
# 1. Создайте класс `Bird`
# 2. Добавьте общий атрибут класса `can_fly` со значением True
# 3. Выведите этот атрибут на экран через обращение к классу

class Bird:
    can_fly = True
print(Bird.can_fly)


# Задача №6 (Тема: Конструктор класса __init__)
# Создайте класс с конструктором:
# 1. Создайте класс `City`
# 2. Добавьте конструктор `__init__`, который принимает параметры `name` и `population`
# 3. Внутри конструктора сохраните их как атрибуты экземпляра
# 4. Создайте объект класса `City` с названием "Москва" и населением 12000000
# 5. Выведите название города и население через атрибуты объекта

class City:
    def __init__(self,name,population ):
        self.name = name
        self.population = population
my_city = City(name="Москва",population=12000000)
print(my_city.name)
print(my_city.population)

# Задача №7 (Тема: Создание класса)
# Создайте еще один пустой класс:
# 1. Создайте класс с именем `Laptop`
# 2. Класс пока не должен содержать никаких атрибутов и методов
# 3. Используйте `pass`

class Laptop:
    pass

# Задача №8 (Тема: Общие атрибуты)
# Создайте класс с общим атрибутом:
# 1. Создайте класс `Fruit`
# 2. Добавьте общий атрибут класса `type` со значением "Фрукт"
# 3. Выведите этот атрибут на экран через обращение к классу

class Fruit:
    type = "Фрукт"
print(Fruit.type)

# Задача №9 (Тема: Конструктор класса __init__)
# Создайте класс с конструктором:
# 1. Создайте класс `Student`
# 2. Добавьте конструктор `__init__`, который принимает параметры `first_name` и `last_name`
# 3. Внутри конструктора сохраните их как атрибуты экземпляра
# 4. Создайте объект класса `Student` с именем "Петр" и фамилией "Петров"
# 5. Выведите полное имя через атрибуты объекта в формате: "Имя: [first_name], Фамилия: [last_name]"

class Student:
    def __init__(self,first_name,last_name):
        self.first_name = first_name
        self.last_name = last_name
fio = Student("Петр","Петров")
print(f"Имя: {fio.first_name}, Фамилия: {fio.last_name}")

# Задача №10 (Тема: Методы класса) - упрощенная
# Создайте класс с простым методом:
# 1. Создайте класс `Dog`
# 2. Добавьте конструктор `__init__` с параметром `name`
# 3. Добавьте метод `bark`, который выводит "Гав!"
# 4. Создайте объект класса `Dog` с именем "Бобик"
# 5. Вызовите метод `bark` у объекта

class Dog:
    def __init__(self,name):
        self.name = name
    def bark(self):
        print("Гав")
my_dog = Dog("Бобик")
my_dog.bark()

# Задача №11 (Тема: Методы класса)
# Создайте класс с методом:
# 1. Создайте класс `Cat`
# 2. Добавьте конструктор `__init__` с параметром `name`
# 3. Добавьте метод `meow`, который выводит "Мяу!"
# 4. Создайте объект класса `Cat` с именем "Мурка"
# 5. Вызовите метод `meow` у объекта

class Cat:
    def __init__(self,name):
        self.name = name
    def meow(self):
        print("Мяу")
my_cat = Cat("Мурка")
my_cat.meow()


# Задача №12 (Тема: Методы класса)
# Создайте класс с методом:
# 1. Создайте класс `Bird`
# 2. Добавьте конструктор `__init__` с параметром `name`
# 3. Добавьте метод `fly`, который выводит "[name] летает!"
# 4. Создайте объект класса `Bird` с именем "Кеша"
# 5. Вызовите метод `fly` у объекта

class Bird:
    def __init__(self,name):
        self.name = name
    def fly(self):
        print(f"{self.name} летает!")
my_bird = Bird("Кеша")
my_bird.fly()

# Задача №13 (Тема: Методы класса)
# Создайте класс с методом:
# 1. Создайте класс `Car`
# 2. Добавьте конструктор `__init__` с параметрами `brand` и `model`
# 3. Добавьте метод `start`, который выводит "[brand] [model] заведена!"
# 4. Создайте объект класса `Car` с брендом "Toyota" и моделью "Camry"
# 5. Вызовите метод `start` у объекта

class Car:
    def __init__(self,brand,model):
        self.brand = brand
        self.model = model
    def start(self):
        print(f"{self.brand} {self.model} заведена!")
my_car = Car(brand="Toyota",model="Camry")
my_car.start()

# Задача №14 (Тема: Методы класса)
# Создайте класс с методом:
# 1. Создайте класс `Pizza`
# 2. Добавьте конструктор `__init__` с параметром `topping` (начинка)
# 3. Добавьте метод `describe`, который выводит "Пицца с [topping]!"
# 4. Создайте объект класса `Pizza` с начинкой "сыр"
# 5. Вызовите метод `describe` у объекта

class Pizza:
    def __init__(self,topping):
        self.topping = topping
    def describe(self):
        print(f"Пицца с {self.topping}!")
my_pizza = Pizza("Сыр")
my_pizza.describe()
