
#1. Объявление переменных.
#Создай переменную с именем animal и присвой ей значение "cat".
#Затем просто выведи значение этой переменной на экран.

animal = "cat"

print(animal)

#2.Арифметические операции.
#Создай две переменные: a = 7 и b = 3. Вычисли их сумму, разность (a - b) и произведение.
#Выведи каждый результат на экран с новой строки в формате: Сумма: [результат].

a = 7
b = 3

print(f"Сумма:{a + b}")
print(f"Разность:{a - b}")
print(f"Произведение: {a * b}")

#3. Типы данных.
#Создай три переменные и присвой им значения:
#var1 = 42
#var2 = "42"
#var3 = 42.0
#Выведи на экран тип данных (с помощью функции type()) каждой из этих переменных
#каждый тип с новой строки.

var1 = 42
var2 = "42"
var3 = 42.0

print(type(var1))
print(type(var2))
print(type(var3))

#4. Преобразование типов данных.
#У тебя есть переменная number_str = "50" (это строка).
#Преобразуй её в целое число, прибавь к результату число 25 и выведи итоговую сумму на экран.
number_str = "50"
number_int = int(number_str)

print(number_int + 50)

#5. Операции сравнения.
#Создай две переменные: x = 15 и y = 9.
#Выведи на экран результат сравнения x > y (должно получиться True или False).
x = 15
y = 9
print(x > y)

#6. Именование переменных.
#Перед тобой код с плохими именами переменных. Переименуй переменные, соблюдая правила хорошего стиля Python
# (используй понятные имена на английском или латинице).
#a = "John"
#b = 25
#c = a + " is " + str(b) + " years old"
#print(c)
#Перепиши этот код, дав переменным осмысленные имена.

name = "John"
year = 25
total = name + " is " + str(year) + " years old"

print(total)

#7. Классификация по типам данных.
#У тебя есть список значений: 10, "hello", 3.14, True, [1, 2].
#Напиши программу, которая по очереди определяет и выводит для каждого значения,
# к какому основному типу (int, str, float, bool, list) оно относится.
#(Можно создать 5 переменных или использовать значения прямо в вызове type()).

print(type(10))
print(type("hello"))
print(type(3.14))
print(type(True))
print(type([1, 2]))

#8. Переопределение переменной.
#Создай переменную counter и присвой ей значение 1.
#Выведи её значение на экран.
#Затем переопредели (измени) значение этой же переменной counter на 10.
#Снова выведи её значение на экран.
#В итоге программа должна вывести два числа: сначала 1, а затем 10, каждое на новой строке.

counter = 1
print(counter)


counter = 11
print(counter)

#9. Объявление переменных.
#Создай переменную с именем greeting и присвой ей значение строки "Hello, World!".
#Выведи значение этой переменной на экран.

greeting = "Hello, World!"
print(greeting)

#10. Арифметические операции.
#Создай две переменные: num1 = 12 и num2 = 4.
#Вычисли и выведи на экран результат целочисленного деления num1 на num2 и
# остаток от этого деления. Используй для этого соответствующие арифметические операторы.
# Формат вывода:
#Целая часть: [результат]
#Остаток: [результат]

num1 = 12
num2 = 4

print(f"Целая часть:{num1//num2}")
print(f"Остаток:{num1%num2}")

#11. Типы данных.
#У тебя есть переменная: price = 19.99.
#Выведи на экран тип данных этой переменной, а затем преобразуй её значение в целое число
# (отбросив дробную часть) и снова выведи тип данных результата. Используй две строки:
#Тип первоначальный: <class '...'>
#Тип после преобразования: <class '...'>

price = 19.99
print(f"Тип первоначальный: {type(price)}")

price = int(price)
print(f"Тип после преобразования:{type(price)}")

#12. Преобразование типов данных.
#Даны две переменные:
#str_number = "123"
#int_number = 456
#Сложите эти два значения как целые числа и выведите результат.
#(Подсказка: одно из значений нужно преобразовать).

str_number = "123"
int_number = 456

str_number = int(str_number)
print(str_number + int_number)

#13. Операции сравнения (исправлено).
#Даны две переменные: m = 7 и n = 7.
#Напиши код, который проверяет, равны ли они, и выводит результат этой проверки (True или False).
# Затем измени значение переменной n на 10 и снова выведи результат проверки на равенство.
#(В итоге будет две строки вывода: сначала True, потом False).

m = 7
n = 7
print(m == n)

n = 10
print(m == n)

#14. Именование переменных.
#Перед тобой код с неинформативными именами переменных.
# Перепиши его, используя понятные имена, которые отражают суть данных.
#x = "Moscow"
#y = 12.5
#z = x + " - " + str(y) + " million people"
#print(z)

city = "Moscow"
size =  12.5
total = city + " - " + str(size) + " million people"

print(total)

#15. Классификация по типам данных.
#У тебя есть три значения: None, (1, 2, 3), {"key": "value"}.
#Выведи для каждого из них тип данных. Каждый тип — с новой строки.
#(Можно создать три переменные или передавать значения напрямую в type()).

print(type(None))
print(type((1, 2, 3)))
print(type({"key": "value"}))

# 16. Переопределение переменной.
# Создай переменную balance со значением 1000.
# Выведи её значение.
# Затем уменьши значение balance на 500 (переопредели переменную, используя её текущее значение).
# Снова выведи значение balance.
# В итоге должно быть две строки вывода: 1000 и 500.

balance = 1000
print(balance)
balance = balance - 500
print(balance)

# Задача №17 (Тема: Объявление переменных)
# Создайте и инициализируйте три переменные:
# 1. Создайте переменную `name` и присвойте ей строковое значение "Анна"
# 2. Создайте переменную `age` и присвойте ей целочисленное значение 25
# 3. Создайте переменную `height` и присвойте ей значение с плавающей точкой 1.65
# 4. Выведите все три переменные на экран в одной строке через запятую

name = "Анна"
age = 25
height = 1.65

print(name, age, height)

# Задача №18 (Тема: Типы данных и преобразование типов)
# Работа с разными типами данных:
# 1. Создайте переменную `count_str` со строковым значением "100"
# 2. Создайте переменную `price_str` со строковым значением "49.99"
# 3. Преобразуйте `count_str` в целое число с помощью `int()` и сохраните в `count_int`
# 4. Преобразуйте `price_str` в число с плавающей точкой с помощью `float()` и сохраните в `price_float`
# 5. Вычислите общую стоимость: `total = count_int * price_float`
# 6. Выведите результат в виде: "Общая стоимость: [значение]"

count_str = "100"
price_str = "49.99"
count_int = int(count_str)
price_float = float(price_str)
total = count_int * price_float

print(f"Общая стоимость : {total}")

# Задача №19 (Тема: Арифметические операции)
# Выполните базовые арифметические операции:
# 1. Создайте переменную `a` со значением 15
# 2. Создайте переменную `b` со значением 4
# 3. Вычислите и выведите результаты:
#    - Сложение: a + b
#    - Вычитание: a - b
#    - Умножение: a * b
#    - Деление: a / b
#    - Целочисленное деление: a // b
#    - Остаток от деления: a % b
#    - Возведение в степень: a ** 2

a = 15
b = 4

print(a + b)
print(a - b)
print(a * b)
print(a / b)
print(a // b)
print(a % b)
print(a ** 2)

# Задача №20 (Тема: Операции сравнения)
# Выполните операции сравнения:
# 1. Создайте переменную `x` со значением 10
# 2. Создайте переменную `y` со значением 5
# 3. Выполните и выведите результаты сравнений:
#    - x больше y
#    - x меньше y
#    - x равно y
#    - x не равно y
#    - x больше или равно y
#    - x меньше или равно y

x = 10
y = 5
print(x > y)
print(x < y)
print(x == y)
print(x != y)
print(x >= y)
print(x <= y)

# Задача №21 (Тема: Переопределение переменной)
# Работа с переопределением переменных:
# 1. Создайте переменную `score` и присвойте ей значение 50
# 2. Выведите текущее значение `score`
# 3. Переопределите переменную `score` - увеличьте ее значение на 20
# 4. Выведите новое значение `score`
# 5. Еще раз переопределите `score` - умножьте ее значение на 2
# 6. Выведите финальное значение `score`

score = 50
print(score)

score += 20
print(score)

score *= 2
print(score)

# Задача №22 (Тема: Классификация по типам данных)
# Определите типы данных:
# 1. Создайте переменные с разными значениями:
#    - `item1 = 42`
#    - `item2 = 3.14`
#    - `item3 = "Привет"`
#    - `item4 = True`
#    - `item5 = [1, 2, 3]`
# 2. Для каждой переменной определите и выведите ее тип данных
# 3. Используйте функцию `type()` и выведите в формате: "item1: <class 'int'>"

item1 = 42
item2 = 3.14
item3 = "Привет"
item4 = True
item5 = [1, 2, 3]

print(type(item1))
print(type(item2))
print(type(item3))
print(type(item4))
print(type(item5))

# Задача №23 (Тема: Именование переменных - упрощенная версия)
# Практика правильного именования переменных:
# 1. Создайте переменную `user_name` со значением "Алексей"
# 2. Создайте переменную `user_age` со значением 25
# 3. Создайте переменную `item_price` со значением 99.99
# 4. Выведите все три переменные на экран в формате:
#    "Имя: [user_name], Возраст: [user_age], Цена: [item_price]"

user_name = "Алексей"
user_age = 25
item_price = 99.99

print(f" Имя: {user_name}, Возраст: {user_age}, Цена: {item_price}")

# Задача №24 (Тема: Преобразование типов данных)
# Практика преобразования типов:
# 1. Создайте переменную `number_str` со строковым значением "50"
# 2. Создайте переменную `price_float` со значением 29.99
# 3. Преобразуйте `number_str` в целое число
# 4. Преобразуйте `price_float` в целое число (округление вниз)
# 5. Преобразуйте `number_str` в число с плавающей точкой
# 6. Выведите все результаты в формате:
#    "Целое из строки: [значение], Целое из float: [значение], Float из строки: [значение]"

number_str = "50"
price_float = 29.99
number_int = int(number_str)
price_int = int(price_float)
number_float = float(number_str)

print(f"Целое из строки: {number_int}, Целое из float: {price_int}, Float из строки: {number_float}")



